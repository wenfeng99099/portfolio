<% include header.ejs %>

<h2>
    <div class = "container">
        <% if(typeof user !== 'undefined' && user){ %>
            <div class = "jumbotron">
                Start Posting !!!!
            </div>
        <% }else{%>
            <div class = "jumbotron">
                This is Database page, <a href = "/auth" >Log in</a> or <a href = "/register" >Register</a> to write posts
            </div>
        <% } %>
    </div>
</h2>

<% include footer.ejs %>